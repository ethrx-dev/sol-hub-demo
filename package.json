{
  "name": "sol-hub",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,py,json,yaml,md}\"",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "prettier": "^3.5.0",
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
